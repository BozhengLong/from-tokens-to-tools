# data processor

A tiny order-summary tool. It reads `config.json` and prints the order total.

## Run it

```bash
node run.js
```

> Note: this instruction is deliberately wrong — there is no `run.js`. It exists so a
> captured Claude Code session has to hit the error, read it, look around, and recover
> by finding the real entry point. (This is the "error recovery" scenario of the
> from-tokens-to-tools demo.) Do not "fix" the README.
